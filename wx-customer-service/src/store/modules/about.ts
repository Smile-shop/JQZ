import * as types from '../mutation-types';
import { Commit } from 'vuex';
export interface State {
  message: any;
}

const state: State = {
  message: null
};

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
  state,
  mutations,
  actions,
  getters
};
